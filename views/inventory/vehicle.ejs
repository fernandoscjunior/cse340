<!--The content Heading-->
<% if (title) { %>
    <h1><%= title %></h1>
<% } else {
    res.redirect('/')
    } %>

<%# messages() %>

<a href="/account/add-cart/<%= locals.inv_id %>">Add to cart</a>

<%- card %>